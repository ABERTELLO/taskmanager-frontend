// Common
import contexts from '../../contexts';


const Notes = () => {
    // const { actions, notes_state, notes_dispatch } = contexts.useNoteContext();

    return (
        <div>
            Notes
        </div>
    );
};

export default Notes;